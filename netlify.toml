[build]
  functions = "netlify/functions"

# Ensure functions work first
[[redirects]]
  from = "/.netlify/functions/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Then catch-all for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
